<br />

***

## 🏗️ 整体架构概览

```
┌─────────────────────────────────────────────────────┐
│                    前端 (Web UI)                      │
│              HTML/CSS/JS SPA + SSE Chat               │
└──────────────────────┬──────────────────────────────┘
                       │ API
┌──────────────────────▼──────────────────────────────┐
│              Agent 7: 助教 (Supervisor)               │
│         路由决策 → 调度其他6个Agent                     │
└──┬─────┬─────┬─────┬─────┬─────┬──────────────────┘
   │     │     │     │     │     │
   ▼     ▼     ▼     ▼     ▼     ▼
 ┌─1─┐ ┌─2─┐ ┌─3─┐ ┌─4─┐ ┌─5─┐ ┌─6─┐
 │学习│ │学习│ │学习│ │个性│ │复习│ │考试│
 │规划│ │专家│ │伙伴│ │出题│ │整理│ │指导│
 └───┘ └───┘ └───┘ └───┘ └───┘ └───┘
   │              ▲      │     │
   │   反馈回路    │      │     │
   └──────────────┘      └─────┘

┌─────────────────────────────────────────────────────┐
│              共享基础设施层                             │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐ │
│  │ 记忆模块  │ │ 工具系统  │ │ 检索模块  │ │状态管理│ │
│  │(Memory)  │ │(Tools)   │ │(Retriever)│ │(State) │ │
│  └──────────┘ └──────────┘ └──────────┘ └────────┘ │
└─────────────────────────────────────────────────────┘
```

***

## 📋 分阶段实施计划

### **第一阶段：基础设施层（先做，所有Agent都依赖它）**

这是最关键的一步，必须**最先完成**，因为所有Agent都依赖这些模块。

| 序号  | 任务           | 说明                                             |
| :-- | :----------- | :--------------------------------------------- |
| 1.1 | 项目结构搭建       | 创建目录结构、配置文件                                    |
| 1.2 | 全局状态Schema定义 | LangGraph的 `TypedDict` State，定义所有Agent共享和传递的字段 |
| 1.3 | 记忆与检索模块      | 短期记忆（对话历史）+ 长期记忆（向量存储）+ Checkpoint             |
| 1.4 | 工具系统基类       | 工具注册机制、基类设计、自定义工具开发                            |
| 1.5 | LLM接入层       | 统一的LLM调用封装（支持切换模型）                             |
| 1.6 | Agent间通信机制   | 基于LangGraph State的消息传递 + 子图调用                  |

**关键设计决策：**

**状态Schema** — 这是整个系统的骨架：

```python
class LearningState(TypedDict):
    messages: Annotated[list, add_messages]        # 对话历史
    user_id: str                                    # 用户标识
    learning_plan: Optional[dict]                   # 学习规划结果
    learning_content: Optional[dict]                # 学习专家输出
    quiz_history: list                              # 做题历史
    error_records: list                             # 错题记录
    weak_points: list                               # 易错点/薄弱点
    focus_time: dict                                # 专注时间统计
    current_agent: str                              # 当前活跃Agent
    agent_outputs: dict                             # 各Agent输出汇总
```

**记忆模块设计：**

- **短期记忆**：LangGraph Checkpoint（自动保存对话状态，支持断点续聊）
- **长期记忆**：向量数据库（Chroma/FAISS）存储用户学习记录、错题、知识文档
- **工作记忆**：State中的字段，Agent间通过State传递上下文

**工具系统设计：**

```python
class BaseTool(ABC):
    name: str
    description: str
    
    @abstractmethod
    def run(self, *args, **kwargs) -> Any: ...
    
    @abstractmethod
    async def arun(self, *args, **kwargs) -> Any: ...

class ToolRegistry:
    _tools: Dict[str, BaseTool] = {}
    
    @classmethod
    def register(cls, tool: BaseTool): ...
    
    @classmethod
    def get_tool(cls, name: str) -> BaseTool: ...
```

***

### **第二阶段：核心Agent构建（自底向上）**

按照**依赖关系**从底层到上层构建：

#### **Agent 1：学习规划（Plan-and-Solve 范式）**

> 为什么选 Plan-and-Solve？因为学习规划本身就是一个"先分析目标→拆解阶段→制定计划"的过程。

```
输入：用户目标（如"我要考研计算机"）
  ↓
[分析节点] 分析用户当前水平、目标、时间
  ↓
[规划节点] 生成阶段性学习方案
  ↓
[反思节点] 检查方案合理性，必要时修正
  ↓
输出：结构化学习方案（表格/文档）
```

**需要的工具：**

- 时间管理工具（计算可用学习时间）
- 知识图谱查询工具（获取学科结构）

**需要的记忆：**

- 用户历史规划（长期记忆）
- 用户基本信息（长期记忆）

***

#### **Agent 2：学习专家（ReAct 范式）**

> 为什么选 ReAct？因为学习专家需要"推理需要什么资料 → 搜索 → 评估结果 → 补充"，是典型的推理-行动循环。

```
输入：学习方案中的某个阶段/主题
  ↓
[推理] 分析该主题需要哪些学习内容
  ↓
[行动] 搜索课程资料、教材推荐
  ↓
[观察] 评估搜索结果的相关性
  ↓
[循环] 直到内容完整
  ↓
输出：分点学习内容 + 推荐课程资料（表格）
```

**需要的工具：**

- 多元搜索工具（Web搜索、课程平台API）
- 文档生成工具（输出表格/文档）

**需要的记忆：**

- 已推荐过的资料（避免重复推荐）
- 用户学习偏好

***

#### **Agent 3：学习伙伴（ReAct + 对话记忆）**

> 为什么选 ReAct？答疑需要"理解问题 → 检索相关知识 → 组织回答"，且需要持续对话。

```
输入：学生问题
  ↓
[推理] 分析问题涉及的知识点
  ↓
[行动] 检索知识库/教材内容
  ↓
[回答] 生成答疑回答
  ↓
[记录] 记录专注时间、问题类型
  ↓
输出：答疑回答 + 专注时间统计
```

**需要的工具：**

- 知识库检索工具（RAG）
- 计时工具（专注时间追踪）

**需要的记忆：**

- 对话历史（短期记忆，Checkpoint）
- 学生常见问题模式（长期记忆）

***

### **第三阶段：评估与反馈Agent**

#### **Agent 4：个性化出题（Reflection 范式）**

> 为什么选 Reflection？出题需要"生成题目 → 评估题目质量 → 修正"，且需要反思学生薄弱点来决定出什么题。

```
输入：学习内容 + 历史记录
  ↓
[分析] 从错题记录中提取薄弱知识点
  ↓
[生成] 根据薄弱点 + 当前学习内容出题
  ↓
[反思] 评估题目难度、区分度是否合适
  ↓
[修正] 调整题目
  ↓
输出：个性化题目 + 能力测评报告
```

**需要的工具：**

- 题目生成工具
- 难度评估工具
- 能力测评工具（IRT模型或简化版）

**需要的记忆：**

- 历史做题记录（长期记忆）
- 错题库（长期记忆）
- 易错点知识库

**Agentic-RL 应用点：**

- 根据学生答题反馈（对/错）动态调整出题策略
- 用强化学习思路优化题目难度梯度：答对→提高难度，答错→降低难度并加强该知识点

***

#### **Agent 5：复习整理（Reflection + 反馈回路）**

> 为什么选 Reflection？需要"分析错题 → 提炼规律 → 反思总结"，且结果要反馈给Agent 1。

```
输入：错题记录 + 学习历史
  ↓
[分析] 归类错题，识别错误模式
  ↓
[提炼] 生成易错点总结
  ↓
[反思] 评估总结的完整性
  ↓
[反馈] 将薄弱点写入State → 触发Agent 1调整规划
  ↓
输出：易错点文档 + 反馈信号
```

**关键：Agent间通信**

- Agent 5 的输出（`weak_points`）写入共享 State
- Agent 1 在下次规划时读取 `weak_points`，调整学习方案
- 这是通过 LangGraph 的 State 传递实现的，不需要额外的消息队列

***

#### **Agent 6：考试和实践指导（Plan-and-Solve 范式）**

```
输入：考试类型/实践项目 + 当前水平
  ↓
[分析] 分析考试要求/项目需求
  ↓
[规划] 制定备考/实践计划
  ↓
[指导] 提供具体策略和注意事项
  ↓
输出：考试/实践指导方案
```

***

### **第四阶段：助教Agent（Supervisor 模式）**

> 这是整个系统的**入口和调度中心**，采用 LangGraph 的 Supervisor 模式。

```
用户消息 → 助教
  ↓
[意图识别] 判断用户需要什么
  ↓
[路由] 分发到对应Agent
  ├── "帮我制定学习计划" → Agent 1
  ├── "推荐学习资料"     → Agent 2
  ├── "我有个问题"       → Agent 3
  ├── "给我出几道题"     → Agent 4
  ├── "帮我整理错题"     → Agent 5
  ├── "考试怎么准备"     → Agent 6
  └── 复合请求 → 多Agent协作
  ↓
[汇总] 收集Agent输出，整合后回复用户
```

**实现方式：** 用 LangGraph 的 `Command(goto=...)` 或条件边实现路由，每个子Agent作为子图（Subgraph）嵌入。

***

### **第五阶段：前端与集成**

| 任务    | 说明                             |
| :---- | :----------------------------- |
| API层  | FastAPI 封装Agent调用，提供REST/SSE接口 |
| 前端    | Streamlit（快速原型）或 Next.js（生产级）  |
| 用户系统  | 登录、学习数据持久化                     |
| 端到端测试 | 完整流程测试                         |

***

## 📁 推荐项目结构

```
software-cup/
├── agent-env/                    # 虚拟环境（已有）
├── src/
│   ├── __init__.py
│   ├── config.py                 # 配置（API keys, 模型选择等）
│   ├── llm.py                    # LLM统一接入层
│   │
│   ├── core/                     # 基础设施层
│   │   ├── __init__.py
│   │   ├── state.py              # 全局State定义
│   │   ├── memory.py             # 记忆模块（短期+长期）
│   │   ├── retriever.py          # 检索模块（RAG）
│   │   ├── tools/
│   │   │   ├── __init__.py
│   │   │   ├── base.py           # 工具基类 + 注册机制
│   │   │   ├── search.py         # 搜索工具
│   │   │   ├── quiz.py           # 出题工具
│   │   │   ├── timer.py          # 计时工具
│   │   │   └── document.py       # 文档生成工具
│   │   └── communication.py      # Agent间通信
│   │
│   ├── agents/                   # 7个Agent
│   │   ├── __init__.py
│   │   ├── planner.py            # Agent 1: 学习规划
│   │   ├── expert.py             # Agent 2: 学习专家
│   │   ├── partner.py            # Agent 3: 学习伙伴
│   │   ├── quizzer.py            # Agent 4: 个性化出题
│   │   ├── reviewer.py           # Agent 5: 复习整理
│   │   ├── examiner.py           # Agent 6: 考试指导
│   │   └── assistant.py          # Agent 7: 助教(Supervisor)
│   │
│   └── api/                      # API层
│       ├── __init__.py
│       └── main.py               # FastAPI应用
│
├── frontend/                     # 前端
├── data/                         # 数据存储
│   ├── vector_store/             # 向量数据库
│   └── checkpoints/              # 检查点存储
│
├── requirements.txt
└── .env                          # 环境变量
```

***

## 🔄 构建顺序总结（严格按此顺序）

```
Phase 1: 基础设施
  1.1 项目结构 + 配置
  1.2 State Schema
  1.3 LLM接入层
  1.4 记忆模块（Memory + Checkpoint）
  1.5 检索模块（RAG）
  1.6 工具系统（基类 + 注册 + 基础工具）

Phase 2: 核心Agent（可并行开发）
  2.1 Agent 1: 学习规划 (Plan-and-Solve)
  2.2 Agent 2: 学习专家 (ReAct)
  2.3 Agent 3: 学习伙伴 (ReAct)

Phase 3: 评估反馈Agent
  3.1 Agent 4: 个性化出题 (Reflection + RL)
  3.2 Agent 5: 复习整理 (Reflection + 反馈回路)

Phase 4: 指导 + 编排
  4.1 Agent 6: 考试指导 (Plan-and-Solve)
  4.2 Agent 7: 助教 (Supervisor)

Phase 5: 集成与前端
  5.1 API层 (FastAPI)
  5.2 前端 (Streamlit/Next.js)
  5.3 端到端测试
```

***

## 💡 关键技术要点

### 1. 上下文工程（Context Engineering）

- **动态Prompt组装**：根据State中的字段动态拼接系统提示词
- **上下文窗口管理**：长对话时使用摘要压缩历史
- **关键信息前置**：将用户画像、当前学习阶段等关键信息放在Prompt开头

### 2. Agent间通信机制

- **主方案**：通过 LangGraph 共享 State 传递数据（`weak_points`、`learning_plan` 等）
- **子图调用**：助教Agent通过 `Command(goto="planner")` 调用子Agent
- **事件驱动**：Agent 5 完成复习整理后，通过State更新触发Agent 1重新规划

### 3. Agentic-RL（强化学习优化）

- **出题策略优化**：Agent 4 根据学生答题反馈动态调整难度
- **奖励函数设计**：答对难题→高奖励，答错简单题→负奖励
- **策略更新**：基于累积反馈调整出题分布

### 4. 工具系统高级特性

- **工具选择**：LLM根据意图自动选择工具（LangGraph原生支持）
- **工具组合**：搜索+过滤+格式化可组合成Pipeline
- **错误处理**：工具调用失败时的重试和降级策略
- **异步执行**：所有工具支持 `arun` 异步调用

***

